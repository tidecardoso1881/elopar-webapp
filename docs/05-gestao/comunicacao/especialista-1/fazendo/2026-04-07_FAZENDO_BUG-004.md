---
para: Especialista 1 (Haiku)
de: Gerente (Cowork)
data: 2026-04-07
tipo: URGENT
bug: BUG-004
branch: fix/bug-004-avatar-upload
---

# URGENT — BUG-004: Upload de foto de perfil quebrado

## O que foi corrigido pelo Gerente (sem PR ainda)

Dois problemas resolvidos:

1. **Bucket `avatars` criado no Supabase** — não existia, causava "Bucket not found"
2. **`src/actions/users.ts` reconstruído** — arquivo estava truncado na linha 60 + `avatar_url` estava comentado (TODO pendente). Gerente corrigiu: descomentou o update de `avatar_url` e fechou o arquivo corretamente.

## Sua tarefa

Apenas commitar os arquivos já corrigidos e abrir PR.

```bash
git checkout main && git pull origin main
git checkout -b fix/bug-004-avatar-upload
npx tsc --noEmit
npm run lint
git add src/actions/users.ts
git commit -m "fix(BUG-004): reconstruir users.ts truncado e ativar persistencia de avatar_url"
git fetch origin && git merge origin/main
gh pr create --title "fix(BUG-004): upload de avatar funcional (bucket + users.ts)" --body "Bucket avatars criado via migration no Supabase. users.ts estava truncado em 60 linhas — reconstruido com avatar_url persistido corretamente no profiles."
```

## DoD

- [ ] `npx tsc --noEmit` zero erros
- [ ] PR aberto com número
