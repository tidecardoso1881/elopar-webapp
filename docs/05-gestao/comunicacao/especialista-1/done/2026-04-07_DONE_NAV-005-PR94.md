# TICKET — EP-NAV-005: Limpeza da Área do Usuário

**Para:** Especialista 1
**De:** Gerente
**Data:** 2026-04-07
**Status:** 📥 INBOX — ⛔ NÃO INICIE antes do PR de NAV-001+002 estar merged

---

## Dependência

Só inicie este ticket após o Tide confirmar o merge de `feat/nav-sidebar-roles`.
As ferramentas admin precisam estar na sidebar antes de removê-las daqui.

---

## Branch

`feat/nav-area-usuario-cleanup` — base `main`

## Arquivo

`src/app/(dashboard)/area-usuario/page.tsx`

---

## O que fazer

Remover os `ToolCard` que agora ficam duplicados na sidebar:

```
REMOVER os ToolCards com estes hrefs:
  - /area-usuario/metricas
  - /area-usuario/saude-testes
  - /area-usuario/gerenciar-usuarios
  - /area-usuario/audit-log

MANTER os ToolCards:
  - /area-usuario/perfil
  - /area-usuario/permissoes
  - qualquer outro relacionado a conta pessoal
```

Atualizar o subtítulo/descrição da página para:
```
"Gerencie seu perfil e configurações pessoais"
```

Não alterar o `<h1>`, não remover as rotas filhas, não tocar em RLS.

---

## Comandos obrigatórios antes do PR

```bash
npx tsc --noEmit
npm run lint
npm run build
git fetch origin && git merge origin/main
```

## DoD

1. Cards de Métricas, Saúde dos Testes, Usuários e Audit Log removidos da `/area-usuario` · cards de Perfil e Permissões mantidos
2. `tsc` + `lint` + `build` sem erros

## Ao concluir

1. PR: `feat(NAV-005): limpeza área do usuário — remove cards duplicados`
2. Mova para `done/` e adicione `PR: #NNN`
3. Avise o Gerente em `gerente/inbox/`
